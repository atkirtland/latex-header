\ProvidesPackage{atkirtland}

% from https://tex.stackexchange.com/questions/195458/implementing-declareunicodecharacter-in-lualatex-an-xelatex

\usepackage{ifxetex,ifluatex}

\newif\ifunicode
\ifxetex\unicodetrue\fi
\ifluatex\unicodetrue\fi

\ifunicode
  \usepackage{fontspec}
  \usepackage{newunicodechar}
  \usepackage{xeCJK}
  \setCJKmainfont{Noto Sans CJK TC}
  \setCJKmonofont{Noto Sans CJK TC}
  \newcommand{\DeclareUnicodeCharacter}[2]{%
    \begingroup\lccode`|=\string"#1\relax
    \lowercase{\endgroup\newunicodechar{|}}{#2}%
  }
\else
  \usepackage[utf8]{inputenc}
\fi

% end from



\usepackage{amssymb}

\usepackage{amsfonts} %used for \mathbb{N}
\usepackage{amsmath}
\usepackage{amsthm} % used for Defn

\usepackage{bbm}
\usepackage{dsfont}

% \usepackage{fullpage}
\usepackage{graphicx,epsfig}
% is this package for pseudocode comments?
\usepackage{comment}
\usepackage{makeidx}
\usepackage{hyperref}

\usepackage{algorithm}
\usepackage{algpseudocode}

\usepackage{tikz}
\usepackage{tikz-cd}
\usetikzlibrary{trees}

\usepackage{young}
\usepackage{youngtab}

% for inclusion map xhookrightarrow
\usepackage{mathtools}

% https://tex.stackexchange.com/questions/60135/path-to-external-files-in-nested-input
\usepackage{import}

% for importing pdfs. use like \includepdf[pages=-]{name.pdf}
\usepackage{pdfpages}

% for code
\usepackage{listings}
\lstset{breaklines=true}

\usepackage{siunitx}
\usepackage[version=4]{mhchem}

% for having horizontal lines that intersect with vertical lines in arrays
% e.g. \hhline{= = | = = # = } where = is each entry, | breaks a single vertical line, # breaks 2 
\usepackage{hhline}

% for markdown
% I'm not sure what the options do, but they were used on the Overleaf site.
% hybrid lets one put latex into the markdown
% https://www.overleaf.com/learn/latex/Articles/How_to_write_in_Markdown_on_Overleaf
\usepackage[hashEnumerators,smartEllipses,hybrid,underscores=false]{markdown}

% for \dd{x}
\usepackage{physics}

% borrowed some ideas from https://github.com/NivenT/latex_header

\theoremstyle{plain}
% \newtheorem{theorem}{Theorem}
\newtheorem*{theorem*}{Theorem}
% \newtheorem{lemma}[thm]{Lemma}
\newtheorem*{lemma*}{Lemma}
% \newtheorem{corollary}[thm]{Corollary}
\newtheorem*{corollary*}{Corollary}
\newtheorem{conjecture}{Conjecture}
\newtheorem*{conjecture*}{Conjecture}
\newtheorem{proposition}{Proposition}
\newtheorem*{proposition*}{Proposition}

\theoremstyle{definition}
% counter shared with thm
\newtheorem*{defn*}{Definition}

\theoremstyle{remark}
\newtheorem*{rmk*}{Remark}
\newtheorem{remark}{Remark}
\newtheorem*{remark*}{Remark}
% \newtheorem{example}[thm]{Example}
\newtheorem{example*}{Example}
% \newtheorem{problem}[thm]{Problem}
\newtheorem*{problem*}{Problem}
\newtheorem*{question*}{Question}
\newtheorem{exercise}{Exercise}
\newtheorem*{exercise*}{Exercise}

\graphicspath{{.}}

\makeindex

\input{mathjax.tex}
\input{unicode.tex}


% use the packages if not using beamer.
% the input envs.tex must come after the theoremstyle plain provided above as it defines the thm env.
\makeatletter
\@ifclassloaded{beamer}{}{
\usepackage[letterpaper, margin=1in]{geometry}
\usepackage{setspace}
\doublespacing{}
\input{envs.tex}
}
\makeatother

% https://tex.stackexchange.com/questions/23102/endinput-what-is-it-for
\endinput
